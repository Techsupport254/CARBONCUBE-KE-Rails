{
  "exclude": [
    "**/node_modules",
    "**/.git",
    "**/log",
    "**/scratch",
    "**/vendor/bundle",
    "**/test",
    "**/spec",
    "**/coverage",
    "db/dumps",
    "config/master.key"
  ],
  "buildAptPackages": ["build-essential", "libjemalloc-dev", "libpq-dev", "libyaml-dev"],
  "steps": {
    "prune:node": {
      "commands": ["rm -rf node_modules && bun install --production"]
    },
    "build_emails": {
      "inputs": [{ "step": "build" }],
      "commands": [
        "bun install --frozen-lockfile",
        "./node_modules/.bin/react-email-rails-build"
      ],
      "deployOutputs": [{ "include": ["tmp/react-email-rails"] }]
    }
  },
  "deploy": {
    "startCommand": "bundle exec puma -C config/puma.rb"
  }
}
